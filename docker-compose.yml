services:
  db:
    image: postgres:16-alpine
    networks:
      - app_net
    environment:
      POSTGRES_USER: spendario
      POSTGRES_PASSWORD: spendario
      POSTGRES_DB: spendario
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U spendario"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: >
      sh -c "
      export PGPASSWORD=$${PGPASSWORD:-spendario} &&
      for i in $$(seq 1 60); do
        pg_isready -h db -p 5432 -U spendario -d spendario && break;
        echo \"Waiting for Postgres...\";
        sleep 1;
      done &&
      poetry run alembic upgrade head &&
      poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
      "
    volumes:
      - ./backend:/app
    environment:
      DATABASE_URL: postgresql+asyncpg://spendario:spendario@db:5432/spendario
      PYTHONUNBUFFERED: "1"
      PGPASSWORD: spendario
    networks:
      - app_net
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    command: npm run dev -- --hostname 0.0.0.0 --port 3000
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8000
      NEXT_TELEMETRY_DISABLED: "1"
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_started
    networks:
      - app_net

volumes:
  pg_data:

networks:
  app_net:
    driver: bridge
